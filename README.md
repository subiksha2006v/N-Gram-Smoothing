# N-Gram-Smoothing
This project implements N-gram smoothing using Python to calculate trigram probabilities from a given text corpus. It uses Add-One (Laplace) smoothing to avoid zero probabilities and analyze word sequences. The program processes sentences, counts trigrams and contexts, and computes probabilities for language modeling tasks.
